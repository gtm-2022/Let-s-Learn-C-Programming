#include<stdio.h>
  int  main()
 { 
    float  a,b,c;
    printf("enter two floating point number :");
    scanf("%f %f", &a,&b);
    c= a*b;
    printf("produt of two floating point number is : %f", c);
    return 0;
 }